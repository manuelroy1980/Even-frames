# run history and scratch — yours, not everyone's
cadence-log.tsv
launch-log.txt
__pycache__/
*.pyc
.DS_Store

# local snapshots of earlier versions
_versions/

# test clips are built on demand, never committed
tests/clips/

# video files should never end up in the repo
*.mp4
*.mov
*.mkv
*.avi
*.webm
*_REPORT.txt
